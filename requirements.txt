# Installed into the Astro Runtime image, which already provides Airflow.
# CPU builds of torch: Docker Desktop on a Mac gives containers no GPU, and the CUDA
# wheels add gigabytes to the image for nothing.
--extra-index-url https://download.pytorch.org/whl/cpu

# Pinned exactly: the host tools and the containers must agree on the DuckDB storage format.
duckdb==1.5.5

anthropic>=0.50
numpy>=1.26
pillow>=10.0
requests>=2.31
torch>=2.5,<3
torchvision>=0.20
