# Copy to .env and fill in. .env is gitignored, and Astro loads it into every container.

# Required by jwst_ingest.
FLICKR_API_KEY=

# Required by jwst_label with backend=api. Nothing else needs it.
ANTHROPIC_API_KEY=
